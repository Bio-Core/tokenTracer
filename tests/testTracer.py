#!/usr/bin/python

import pytest

from tokenTracer import tokenTracer


def test1():
    args = 
    sniffFrontend.sniff(args)


def test2():
    packet = pyshark.packet()
    pDict = packetDict(False, False, False, False)
    pDict.loadAll(packet)

